import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Resetar a senha | Dogs',
  description: 'Resete a sua senha no Dogs',
};

export default async function ResetarPage() {
  return (
    <main>
      <h1>Resetar</h1>
    </main>
  );
}
